version: '3.8'
services:
  bot:
    container_name: OnceUponAMime
    image: theprogrammershangout/once-upon-a-mime:latest
    volumes:
      - type: bind
        source: ./config.json
        target: /config.json
    restart: always
    env_file:
      - .env